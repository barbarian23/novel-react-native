import React from 'react';
import {memo} from 'react';
import {View} from 'react-native';

const TabComment = () => {
  return (
    <View style={{flex: 1}}>

    </View>
  );
};

export default memo(TabComment);
